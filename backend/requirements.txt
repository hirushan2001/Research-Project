fastapi>=0.100.0
uvicorn>=0.22.0
python-multipart>=0.0.6
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0.0

# Note: inference-sdk is commented out here because it does not support Python 3.13 yet.
# The backend application will run in simulated mode on Python 3.13+.
# For live connections on Python <3.13, uncomment and install:
# inference-sdk>=0.9.11
